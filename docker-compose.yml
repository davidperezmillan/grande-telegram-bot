services:
  grande-bot:
    build: .
    container_name: grande_bot
    restart: unless-stopped
    user: "1000:1000"
    environment:
      - API_ID=${API_ID}
      - API_HASH=${API_HASH}
      - BOT_TOKEN=${BOT_TOKEN}
      - USER_ID=${USER_ID}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./downloads:/app/downloads
    networks:
      - grande-bot-net

networks:
  grande-bot-net:
    driver: bridge